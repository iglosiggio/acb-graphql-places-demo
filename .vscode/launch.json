{
    "version": "0.2.0",
    "configurations": [
      {
        "type": "mule-xml-debugger",
        "request": "launch",
        "name": "Debug Mule Application",
        "mule.project": "${workspaceFolder}",
        "mule.home": "${config:mule.app.runtime.muleHome}",
        "mule.runtime.args": "${config:mule.app.runtime.defaultArguments}"
      }
    ]
  }